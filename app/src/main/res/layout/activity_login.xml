<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_login"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/background"
    tools:context="com.example.user.androidproject.LoginActivity">
    <!--android:background="@drawable/login_bg">-->

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center"
        android:layout_gravity="center"
        android:layout_above="@+id/loginBtn"
        android:layout_marginBottom="120dp"
        android:layout_centerHorizontal="true">

        <ImageView
            android:layout_width="100dp"
            android:layout_height="100dp"
            android:src="@drawable/login_icon"/>
        <TextView
            android:layout_marginTop="20dp"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="You're readty to go!"
            android:textColor="#000000"
            android:textStyle="bold"
            android:textSize="22dp" />
        <TextView
            android:layout_marginTop="10dp"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Choice your today's menu."
            android:textSize="16dp"/>
    </LinearLayout>

    <ImageView
        android:id="@+id/loginBtn"
        android:layout_width="283dp"
        android:layout_height="56dp"
        android:src="@drawable/facebook_login2"
        android:layout_marginBottom="7dp"
        android:layout_above="@+id/startBtn"
        android:layout_centerHorizontal="true"
        android:layout_alignStart="@+id/startBtn" />

    <ImageView
        android:id="@+id/startBtn"
        android:layout_width="283dp"
        android:layout_height="56dp"
        android:src="@drawable/signin_login2"
        android:layout_marginBottom="20dp"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true" />

</RelativeLayout>
